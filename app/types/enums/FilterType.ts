enum FilterType {
  String = 'string',
  Number = 'number',
  Boolean = 'boolean',
  Date = 'date',
  DateTime = 'datetime',
  Time = 'time',
  Array = 'array',
  Object = 'object'
}

export default FilterType;
